#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N;
    int W;
    cin >> N >> W;
    long int x;
    long long oisisa = 0;
    vector<pair<long long, long long>> v(N);
    for(auto &nx)
}